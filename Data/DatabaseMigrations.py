import OneTimeBuildScripts as build
import sqlite3 as sql

# build.populateLeagues()
# build.populateTeams()
# build.buildPlayers()

# build.buildPositions()
# build.populatePositions()
build.addPlayerPositions()